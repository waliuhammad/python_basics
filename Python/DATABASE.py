import mysql.connector
mydb = mysql.connector.connect(
    host = "localhost",
    user = "Wali Muhammad",
    password = "#Includestdio.h",
    database = "User Management"
)

print("Database connection established successfully..")
